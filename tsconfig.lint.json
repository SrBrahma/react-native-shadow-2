// We need this file for eslint, as in tsconfig.json we exclude the tests from the compilation,
// but then, jest will complain the test files aren't included in tsconfig.
{
  "extends": "./tsconfig.json",
  "exclude": ["lib", "node_modules"] // Same as tsconfig but without excluding test files
}